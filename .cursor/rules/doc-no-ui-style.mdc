---
description: 需求文档禁止写 UI 样式，留给设计师空间
globs: docs/miniprogram/pages/**/*.md
alwaysApply: true
---

# 铁律：需求文档不写 UI 样式

需求文档（`docs/miniprogram/pages/*.md`）与右侧需求预览，写**功能、逻辑、文案、状态、跳转、权限**，不写视觉样式。样式留给 UI 设计师；原型视觉以预览/截图为准，不必再用文字规定观感。

## 禁止写入（除非产品明确要求「必须这样」的特例）

| 类型 | 示例（勿写） |
|------|-------------|
| 颜色 | 主色、橙色、粉色、绿色、蓝色、灰字、白字、黄底、橙底、浅蓝底 |
| 字重/字号 | 加粗、细体、字号、标题加粗 |
| 形状/材质 | 圆角、方形圆角、阴影、渐变、描边、白底卡片 |
| 弱化/强调观感 | 灰化、去色、灰度、降对比、浅灰底、弱化展示 |
| 布局观感套话 | 横卡同构、左右留边、首卡上移压封面、彼此间距（无业务含义时） |

## 允许写

- 字段与文案：展示什么、按钮叫什么、空态提示原文
- 结构与信息优先级：有封面、有类型标签、底栏有操作（不规定长什么样）
- 状态与规则：已结束 vs 进行中如何区分（用文案/是否可点说明，不用「灰化」）
- 交互：吸顶、筛选默认不勾选、点卡片进详情
- **特例**：产品书面要求必须固定的视觉约束（须写清「特例原因」）

## 改文档时

1. 新增/补充需求时，默认不写样式句。
2. 发现已有样式描述：删掉或改成功能表述（例如「主色按钮」→「可点的「已报名人员」」；「卡片灰化」→「已结束与进行中区分展示」）。
3. 不借机大扫除无关章节；只改当前任务相关段落，或用户明确要求「全局清样式」时再通篇清理。

## 与原型的关系

- 原型/预览可以有完整视觉；**文档不复述**颜色、圆角、字重等。
- 需要给设计对照时，用截图 `![]()`，不要用文字规定样式。
