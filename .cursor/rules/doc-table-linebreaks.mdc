---
description: 需求文档与预览表格中的分点说明必须换行显示
alwaysApply: true
---

# 铁律：表格分点说明必须换行

需求文档（`docs/**/*.md`）以及右侧需求预览里，**表格单元格中的分点说明，每个点必须单独一行**，便于阅读。

## 写作（真源 md）

- 单元格内若有多条分点（`1、` `2、` / `1.` `2.` 等），**点与点之间必须用 `<br>` 断开**。
- 正确示例：`1、浅蓝渐变圆角卡片<br>2、头像<br>3、昵称`
- 错误示例：`1、浅蓝渐变圆角卡片 2、头像 3、昵称`（挤在同一行）
- 写新表、改旧表时都遵守；发现挤在一行的分点应顺手拆开（仅改当前相关表格，不借机大扫除）。

## 预览

- 右侧面板对表格单元格会按 `<br>` 与「`1、` / `1.1、`」等序号**自动断行**（序号前即使无空格也会断）。
- **禁止**把 `1.1、` 拆成 `1` + `.1`；子序号须整段保留。
- 仍以 md 里显式 `<br>` 为准；不要用不可见控制符（如 `\x1f`）代替 `<br>`。
- 分点序号须连续正确（如 1、2、3；子点 1.1、1.2），不要出现两个「3、」。

## 范围

- 适用于所有需求文档表格（不限「说明」列名），凡单元格内多条分点均换行。
- 正文列表（`-` / 独立段落）本身已换行，不在此限。
