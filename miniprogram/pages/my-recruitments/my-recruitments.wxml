<view class="page my-recruit">
  <view class="my-recruit__tabs">
    <view
      wx:for="{{tabs}}"
      wx:key="key"
      class="my-recruit__tab {{activeTab === item.key ? 'my-recruit__tab--active' : ''}}"
      data-key="{{item.key}}"
      bindtap="onTabChange"
    >{{item.display}}</view>
  </view>

  <view class="my-recruit__overview" wx:if="{{activeTab !== 'draft'}}">
    <view class="my-recruit__stat-card my-recruit__stat-card--signup">
      <text class="my-recruit__stat-num">{{monthStats.signups}}</text>
      <text class="my-recruit__stat-label">本月报名人次</text>
    </view>
    <view class="my-recruit__stat-card my-recruit__stat-card--income">
      <text class="my-recruit__stat-num">¥{{monthStats.income}}</text>
      <text class="my-recruit__stat-label">本月收入</text>
    </view>
  </view>

  <view class="my-recruit__list">
    <block wx:for="{{currentList}}" wx:key="recruit_id">
      <view wx:if="{{item.actionType === 'draft'}}" class="my-recruit__card my-recruit__card--draft">
        <view class="my-recruit__draft-head">
          <view class="my-recruit__draft-icon-wrap">📝</view>
          <view class="my-recruit__draft-body">
            <text class="my-recruit__title">{{item.title}}</text>
            <text class="my-recruit__draft-hint">未发布 · 信息待完善</text>
          </view>
          <text class="my-recruit__badge my-recruit__badge--draft">{{item.badgeLabel}}</text>
        </view>
        <view class="my-recruit__draft-meta">
          <view class="my-recruit__row" wx:if="{{item.location}}">
            <text class="my-recruit__row-icon">📍</text>
            <text class="my-recruit__row-text">{{item.location}}</text>
          </view>
          <text class="my-recruit__draft-plan">计划招募 {{item.total}} 人 · ¥{{item.fee}}/人</text>
        </view>
        <view class="my-recruit__actions my-recruit__actions--draft">
          <view class="my-recruit__btn my-recruit__btn--primary" data-id="{{item.recruit_id}}" bindtap="onEditDraft">继续编辑</view>
          <view class="my-recruit__btn my-recruit__btn--ghost" data-id="{{item.recruit_id}}" data-title="{{item.title}}" bindtap="onDeleteDraft">删除</view>
        </view>
      </view>

      <view wx:else class="my-recruit__card" data-id="{{item.recruit_id}}" bindtap="onViewDetail">
      <view class="my-recruit__card-head">
        <text class="my-recruit__title">{{item.title}}</text>
        <text class="my-recruit__badge my-recruit__badge--{{item.badgeType}}">{{item.badgeLabel}}</text>
      </view>

      <view class="my-recruit__row">
        <text class="my-recruit__row-icon">📅</text>
        <text class="my-recruit__row-text">{{item.timeDisplay}}</text>
      </view>
      <view class="my-recruit__row">
        <text class="my-recruit__row-icon">📍</text>
        <text class="my-recruit__row-text">{{item.location}}</text>
      </view>

      <view class="my-recruit__signup-row">
        <view class="my-recruit__signup-main">
          <text class="my-recruit__signup-text">已报 {{item.signed}}/{{item.total}} 人</text>
          <view class="my-recruit__bar">
            <view class="my-recruit__bar-inner" style="width: {{item.progress}}%;"></view>
          </view>
        </view>
        <text class="my-recruit__fee {{item.badgeType === 'closed' || item.badgeType === 'ended' ? 'my-recruit__fee--muted' : ''}}">¥{{item.fee}}/人</text>
      </view>

      <view class="my-recruit__actions" wx:if="{{item.actionType === 'active'}}">
        <view class="my-recruit__btn my-recruit__btn--primary" data-title="{{item.title}}" catchtap="onViewSignup">
          <text class="my-recruit__btn-icon">👥</text>
          <text>查看报名</text>
        </view>
        <view class="my-recruit__btn my-recruit__btn--outline" data-id="{{item.recruit_id}}" catchtap="onEdit">
          <text class="my-recruit__btn-icon">✎</text>
          <text>编辑</text>
        </view>
        <view class="my-recruit__btn my-recruit__btn--danger" data-id="{{item.recruit_id}}" data-title="{{item.title}}" catchtap="onUnpublish">
          <text>下架</text>
        </view>
      </view>

      <view class="my-recruit__actions" wx:elif="{{item.actionType === 'closed'}}">
        <view class="my-recruit__btn my-recruit__btn--primary" data-title="{{item.title}}" catchtap="onViewSignup">
          <text class="my-recruit__btn-icon">📋</text>
          <text>查看名单</text>
        </view>
        <view class="my-recruit__btn my-recruit__btn--outline" data-id="{{item.recruit_id}}" catchtap="onViewData">
          <text class="my-recruit__btn-icon">📊</text>
          <text>数据</text>
        </view>
      </view>

      </view>
    </block>

    <view wx:if="{{currentList.length === 0}}" class="my-recruit__empty">
      <text class="my-recruit__empty-icon">{{emptyState.icon}}</text>
      <text class="my-recruit__empty-title">{{emptyState.title}}</text>
      <text class="my-recruit__empty-hint">{{emptyState.hint}}</text>
      <view
        wx:if="{{emptyState.actionText}}"
        class="my-recruit__empty-btn"
        bindtap="onEmptyAction"
      >{{emptyState.actionText}}</view>
    </view>
  </view>
</view>
