---
description: 需求文档当前逻辑存在矛盾、不一致或遗漏时，在文档底部维护待确认事项
alwaysApply: true
---

# 需求文档待确认事项

适用于 `docs/miniprogram/pages/*.md`。

## 每次改文档后必须检查

检查当前文档需求正文是否存在：

- `矛盾`：同一逻辑出现互斥描述
- `不一致`：同一字段、状态、操作或取值在不同章节描述不同
- `遗漏`：已有流程缺少必要条件、状态、结果或异常处理

判断优先级：**正文当前需求 > 补充说明 > 变更记录/历史记录**。历史记录与当前正文不同不算矛盾，只检查当前需求正文内部。

## 写入格式

发现问题时，在文档最底部维护：

```markdown
## 待确认事项

- [ ] 矛盾：§3.2 与 §3.5 对同一操作描述不同，请明确……
- [ ] 不一致：……
- [ ] 遗漏：……
```

- 每条写清类型、涉及章节、冲突点或缺失点，以及需要用户明确的问题。
- 底部待办禁止使用会标红的 `【矛盾】` 等标记；已有底部红色标记内容直接从 Markdown 真源删除。
- 不替用户决定答案，不根据文档或待办反向修改原型。
- 未勾选的 `[ ]` 条目必须保留；新增检查结果不得覆盖旧条目。
- 用户明确补充并勾选为 `[x]` 后，将结论写入对应正文，再删除该条。
- 没有未处理事项时，删除空的「待确认事项」章节。
- 不使用自动扫描脚本；由 AI 进行语义检查。
