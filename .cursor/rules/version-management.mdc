---
description: 网站版本管理与发布日志更新规范
globs:
  - VERSION
  - docs/版本发布日志.md
alwaysApply: true
---

# 版本管理

用户不自行写代码；版本管理由 AI 负责。

## 文件

| 文件 | 作用 |
|------|------|
| `VERSION` | 当前线上版本号（如 `1.0.0`） |
| `docs/版本发布日志.md` | 每次正式发布后的变更记录 |

## 发布流程（必须按顺序）

1. 完成网站修改，本地预览确认
2. `git add` → `git commit`（commit message 说明变更）
3. `git push origin main`，等待 GitHub Actions 部署成功
4. 验证线上 URL 返回 HTTP 200：https://cgd1093702532-star.github.io/personal-site/
5. **仅在线上确认成功后**：
   - 按 SemVer 更新 `VERSION`
   - 在 `docs/版本发布日志.md` 顶部追加新版本条目（含日期、变更摘要、Git 标签、提交 hash）
   - `git tag vX.Y.Z` 并 `git push origin vX.Y.Z`
   - 再次 commit + push 版本文件与日志

## 版本号决策

- 文案/样式小改 → PATCH +1
- 新区块或明显功能 → MINOR +1，PATCH 归零
- 大改版 → MAJOR +1，其余归零

## 禁止

- 未验证线上可访问就更新发布日志
- 跳过 `VERSION` 或发布日志直接打 tag
- 用户未要求时主动 push

## 用户常用指令

- 「推送到 GitHub」→ 执行发布流程 2~5
- 「发布 v1.0.1」→ 使用指定版本号
- 「查看版本日志」→ 读取 `docs/版本发布日志.md`
