---
description: 铁律：只根据原型改动写入需求文档；禁止按文档去改原型
alwaysApply: true
---

# 铁律：原型 → 需求文档（单向）

## 方向（必须）

| 允许 | 禁止 |
|------|------|
| 用户改原型 / 你按用户对原型的指令改预览与小程序 | **根据需求文档里的文字去改原型**（「文档怎么写就怎么改界面」） |
| 原型改完后，把**本次改动涉及的逻辑**自动**写入**对应 `docs/miniprogram/pages/*.md` | 读完文档后主动「对齐实现」、按旧文档改回原型 |
| 对文档只做**写入 / 补充** | 用文档当施工图去驱动原型重构 |

**一句话：文档只进不出驱动；原型改了再写进文档。**

## 改原型时

1. 以用户对预览/DOM/交互的说明为准，改 `preview/`、`miniprogram/` 等实现。
2. **不要**为了「和 md 一致」而改界面、文案、字段、流程。
3. 若文档与当前原型不一致：在文档里用 `【矛盾】` / `【重复】` 标出（见下），**等用户确认**；仍不改原型去迁就旧文档。

## 写需求文档时（只写）

1. 原型变更落地后，在对应页面 md **补充**本次涉及的逻辑、字段、交互、验收点。
2. **保留用户已写原文**；新信息追加段落 / 新表行 / 变更记录（变更记录超过 7 条时只留最近 6 条 + 最早 1 条）；不要整段覆盖用户句子。
3. 保留用户的 `![]()` 截图语法；**禁止**改成纯文字（反复导致右侧无图，见 `doc-embedded-images.mdc`）。
4. 写完后按需 `python3 preview/build-pages.py`，保证右侧预览与真源一致（见 `docs-preview-sync.mdc`）。

## 彩色标记（右侧预览着色）

```markdown
> 【重复】……（两边都保留，供产品删并）

> 【矛盾】……（与现行原型或他节冲突，请确认；确认前不改原型迁就文档）
```

| 标签 | 预览色 |
|------|--------|
| `【重复】` | 紫 |
| `【矛盾】` | 红 |
| `【特别说明】` | 橙 |

## 禁止（再强调）

- 「文档写了宫格，所以把原型斜切改回宫格」这类 **文档驱动原型**
- 改完原型却不写文档
- 为对齐实现而覆盖用户刚写的需求句
- 发现矛盾后静默删掉文档或原型一侧且不留 `【矛盾】` 标记
