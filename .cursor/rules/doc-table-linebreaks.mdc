---
description: 铁律：需求文档与预览表格中的序号分点必须永久换行显示
alwaysApply: true
---

# 铁律：表格序号分点必须换行（永久）

需求文档（`docs/**/*.md`）以及右侧需求预览里，**表格单元格中凡按序号排列的分点，每个点必须单独一行**。  
这是**永久铁律**：预览渲染与真源写作都不得回退成挤在同一行。

## 序号形态（均须换行）

| 形态 | 示例 |
|------|------|
| 中文顿号序号 | `1、` `2、` `3、` |
| 子序号 | `1.1、` `1.2、` |
| 西式序号 | `1. ` `2. `（点号后有空白） |
| 场景序号 | `场景 1：` `场景 2：` |

## 写作（真源 md · 必须）

- **真源也要换行**：单元格内多条序号分点，点与点之间**必须**写显式 `<br>`（不能只靠预览自动断行）。
- 正确：`1、浅蓝渐变圆角卡片<br>2、头像<br>3、昵称`
- 正确：`场景 1：…<br>场景 2：…<br>1、触发节点：…`
- 错误：`1、浅蓝渐变圆角卡片 2、头像 3、昵称`（挤在同一行）
- **禁止**用不可见控制符（如 `\x1f`）代替 `<br>`。
- 写新表、改旧表时都遵守；发现挤在一行的分点应拆开（只改当前相关表格，不借机大扫除）。
- 分点序号须连续正确（如 1、2、3；子点 1.1、1.2），不要出现两个「3、」。

## 预览（永久行为）

- 右侧面板对表格单元格**必须**按 `<br>` 与上述序号形态**自动断行**（序号前即使无空格也会断）。
- **禁止**把 `1.1、` 拆成 `1` + `.1`；子序号须整段保留。
- **禁止**弱化、删除或绕过 `preview-doc-aside.js` 的 `formatTableCell` 断行逻辑。
- 仍以 md 里显式 `<br>` 为写作真源；预览自动断行是兜底，不能代替真源写 `<br>`。
- 正文中的子序号段落（如 `4.1、` `4.2、`）预览须做首行缩进（`preview-doc-aside__p--sub`）。

## 自检

```bash
python3 scripts/check-doc-table-linebreaks.py --fix   # 把挤在一行的序号拆成 <br>
python3 scripts/check-doc-table-linebreaks.py         # 须 OK
```

`preview/build-pages.py` 已接入上述检查。

## 范围

- 适用于所有需求文档表格（不限「说明」列名），凡单元格内多条序号分点均换行。
- 正文列表（`-` / 独立段落）本身已换行，不在此限。
