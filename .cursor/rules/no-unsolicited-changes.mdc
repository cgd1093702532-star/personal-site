---
description: 禁止擅自改动已实现内容；不确定先问用户
alwaysApply: true
---

# 铁律：不擅自调整已实现内容

执行用户任务时，**只做明确要求的事**，不要自行扩展、回滚、恢复或「顺手优化」已有实现。

## 必须遵守

1. **范围克制**：只改用户当前指令直接相关的文件与行为；不要连带改无关文案、入口、文档、截图、已删功能。
2. **不确定就问**：需求有歧义、多种做法、或牵涉是否恢复/删除已有能力时，**先问用户再动手**，禁止自行拍板。
3. **禁止擅自恢复**：不要因为「以前有过」就恢复已删除的页面、入口、弹层、文档或依赖，除非用户明确要求。
4. **禁止擅自推翻**：不要把用户已拍板或当前已上线的交互（如直达 vs 弹层、文案、门禁）改成你觉得「更合理」的版本。
5. **禁止顺手重构**：不为「完整性」「一致性」主动改周边模块；需要同步时先确认。

## 允许

- 为完成**明确指令**所必需的最小改动（例如改文案时同步预览与小程序同一处）。
- 用户明确要求「同步文档 / 截图 / 构建」时再做这些收尾。

## 反例（禁止）

- 用户只说改入口文案 → 却自行恢复已删除的发布课程页、改多份文档、重拍截图一整套。
- 用户说「还要底部选项」→ 却自行决定选项文案、跳转目标、是否重建整页，而不先问。
